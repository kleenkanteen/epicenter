{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "Local Mail main window. It loads the engine's external loopback origin (a remote http:// page) and never calls Tauri IPC, so it is granted no permissions: capabilities apply to local app content, and this window has none.",
	"windows": ["main"],
	"permissions": []
}
