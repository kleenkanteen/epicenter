{
	"name": "@epicenter/ops",
	"private": true,
	"type": "module",
	"scripts": {
		"cf:plan:remote": "infisical run --env=prod --path=/ops -- bun run cf/apply.ts --plan",
		"cf:apply:remote": "infisical run --env=prod --path=/ops -- bun run cf/apply.ts",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@epicenter/constants": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "catalog:",
		"typescript": "catalog:"
	}
}
